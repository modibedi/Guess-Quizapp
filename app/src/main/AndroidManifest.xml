<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ekasi.guess">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Quiz">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Logon" />
        <activity android:name=".Currentaffairs" />
        <activity android:name=".Sports" />
        <activity android:name=".Entertainment" />
        <activity android:name=".SportsResults" />
        <activity android:name=".CurrentAffairsResults" />
        <activity android:name=".EntertainmentResults"></activity>
    </application>

</manifest>